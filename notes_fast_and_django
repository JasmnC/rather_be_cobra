
FastAPI
What is fastapi: it's a python framework, to build api quickly

Explain the difference between def and async def in FastAPI route handlers. When should you use each?
- they are route handlers
- def is automatically run in a separate thread pool -> good for CPU-bound operation
- async def is coroutine and can be safely pause and resume -> good for I/O-bound operation

What is dependency injection in FastAPI and how does it work behind the scene?

How does FastAPI achieve automatic API documentation?

Explain the difference between Path, Query, Header, Body, and Cookie parameters in FastAPI.

What is the purpose of Pydantic models in FastAPI? How do they differ from SQLAlchemy/SQLModel database models?

Explain how FastAPI's dependency caching works within a single request. Why is this important?

How does FastAPI handle request validation and what happens when validation fails? How can you customize error responses?

Explain the difference between using Annotated[Session, Depends(get_db)] vs Session = Depends(get_db) for type hints. Which is recommended and why?

What's the difference between using requests and httpx for making HTTP calls in FastAPI, and why is httpx preferred in async contexts?
- Requests HTTP does not support async requests


What are the key differences in the database URL connection string when migrating from sync SQLAlchemy to async SQLAlchemy, and what driver changes are required for PostgreSQL?

What is ASGI (Asynchronous Server Gateway Interface) and how does it differ from WSGI?

How is FastAPI different from Flask?

Explain the difference between response_model, response_model_exclude, and response_model_include in FastAPI route decorators. When would you use each?

How do you implement JWT authentication in FastAPI

How does FastAPI handle synchronous functions differently?

Django
What is Django's MTV pattern?

What's the difference between blank=True and null=True?

What's the difference between auto_now and auto_now_add?

What are Django migrations and why are they important?

What is the N+1 query problem? How to avoid it in Django

What is the Meta class in Django models?

What's the purpose of str() method in models?
